# CLAUDE.md — gh-pages branch

## Branch Purpose

This branch (`gh-pages`) is **exclusively** for the project website. It is deployed via GitHub Pages. The ML pipeline code lives on `main` — do not port or reference that code here.

The site is in early development. Currently there is a template  in `index.html` and some content in `index.md`. The content in `index.md` will  be edited and eventually moved to index.html.
